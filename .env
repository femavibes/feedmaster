# Database connection URL (This is what your Python code expects)
DATABASE_URL="postgresql://user:password@db:5432/feedmaster_db"

# Worker and Aggregation Intervals (from previous warnings)
WORKER_POLLING_INTERVAL_SECONDS=60
AGGREGATION_INTERVAL_MINUTES=5
PROMINENT_DID_REFRESH_INTERVAL_MINUTES=30

# Bluesky API for DID resolution
BLUESKY_API_BASE_URL="https://api.bsky.app/xrpc"

# Secret key for JWT token generation (replace with a strong, random string!)
SECRET_KEY="your-super-secret-key-replace-me"

# Algorithm for JWT (HS256 is common)
ALGORITHM="HS256"

# Token expiration time in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Optional: If you had SQLALCHEMY_DATABASE_URL defined elsewhere and your code *didn't* use DATABASE_URL,
# you could keep it, but it seems unnecessary given the traceback.
# SQLALCHEMY_DATABASE_URL="postgresql://user:password@db:5432/feedmaster_db" # Consider removing if not used